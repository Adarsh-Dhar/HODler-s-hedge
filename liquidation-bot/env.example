# Liquidator wallet private key (NEVER commit this to git!)
# Generate a new wallet specifically for the liquidation bot
LIQUIDATOR_PRIVATE_KEY=0x...

# RPC URL for Mezo Testnet (optional override)
RPC_URL=https://rpc.test.mezo.org

# TradingEngine contract address (optional - defaults to lib/address.ts)
# Only set this if you need to override the default address from lib/address.ts
# TRADING_ENGINE_ADDRESS=0x304B0E3DFC3701F5907dcb955E93a9D7c8b78b7F

# Monitoring interval in milliseconds (default: 15000 = 15 seconds)
MONITOR_INTERVAL_MS=15000

# Chain ID for Mezo Testnet
CHAIN_ID=31611

# Optional: Maximum gas price in gwei (0 = no limit)
MAX_GAS_PRICE_GWEI=0

# Optional: Number of blocks to look back for backfilling positions (default: ~7 days)
BACKFILL_BLOCK_RANGE=6000

